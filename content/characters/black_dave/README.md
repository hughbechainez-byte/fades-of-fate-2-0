# Black Dave (2.0)

Authored pose / animation sources retained for 2.0.

Integrate into OpenBOR character format under `openbor/chars/` when implementing playable Dave — do not load FoF1 `src/` runtime as the product.
