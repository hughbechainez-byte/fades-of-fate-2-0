# OpenBOR data

The initial data registry is now present: `game.txt`, `models.txt`, `levels.txt`, and the I-8 underpass level shell. Black Dave is registered from `chars/black_dave/`.

The OpenBOR executable/toolchain is not checked into this repository; use `tools/Validate-OpenBOR-Black-Dave.py` for deterministic content validation until an engine runtime is available.
